# Raspberry Pi 4 boot config for bare metal kernel
# Copy this file to the boot partition of your SD card (same level as kernel8.img).

# 64-bit kernel
arm_64bit=1

# Our kernel
kernel=kernel8.img

# Enable UART for serial output (optional; helps with some cables)
enable_uart=1
